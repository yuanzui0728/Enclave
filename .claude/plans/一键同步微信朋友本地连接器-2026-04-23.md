# 一键同步微信朋友本地连接器完整实现规划

日期：2026-04-23

## 目标

把管理后台「一键同步微信朋友」从当前的“预留 HTTP 连接器 + 手动 JSON 导入”推进到可本地真实使用的完整能力：

- 本地启动一个 `127.0.0.1:17364` 微信连接器服务。
- 管理后台可探测连接器、扫描本地微信数据源、列出联系人、批量生成 `WechatSyncContactBundle[]`。
- 用户确认后复用现有 `/api/admin/wechat-sync/preview` 与 `/api/admin/wechat-sync/import`，生成隐界角色并建立好友关系。
- 全链路默认本地化、显式授权、可审计、可回滚，不把原始聊天记录上传到远端。

## 现状判断

当前仓库已有：

- `apps/admin/src/lib/wechat-local-connector.ts`：前端 HTTP 客户端，默认连接 `http://127.0.0.1:17364`。
- `apps/admin/src/routes/wechat-sync-page.tsx`：管理后台页面，支持连接器读取和手动 JSON 导入。
- `api/src/modules/admin/wechat-sync-admin.service.ts`：接收联系人快照，生成角色预览、导入角色、建立好友关系、记录导入快照和回滚。
- `packages/contracts/src/wechat-sync.ts`：`WechatSyncContactBundle` 契约。

当前仓库缺失：

- 没有真正监听 `17364` 的本地连接器服务。
- 没有微信数据源发现、解密、导出、摘要、联系人聚合逻辑。
- 没有本地连接器启动脚本、状态管理、日志、测试夹具。

## 外部项目取舍

参考项目：

- `ylytdeng/wechat-decrypt`：微信 4.x 本地数据库解密工具，可从运行中的微信进程内存提取 SQLCipher 4 密钥，解密数据库并提供 Web UI / HTTP API。
- `hellodigua/ChatLab`：本地聊天记录分析桌面 App，Electron/Vue 架构，支持 WeChat 等多平台导入与统一数据模型。

取舍结论：

- 不直接拷贝 `ChatLab` 代码。它是 AGPL-3.0，直接集成会把许可证义务带进本项目，除非后续明确接受 AGPL 传播边界。
- 不直接拷贝 `wechat-decrypt` 代码。当前公开仓库未在仓库根部清晰暴露可复用许可证文件，不能默认 vendor。
- 可以做“协议适配”：用户外部安装并运行 `wechat-decrypt`，我们的连接器调用它的本地 API 或读取它生成的用户授权导出结果。
- 可以参考 `ChatLab` 的“统一聊天数据模型”思想，但自己定义并实现 `WechatSyncContactBundle` 生成器。

## 产品边界

必须做：

- 本地服务只绑定 `127.0.0.1`，不监听局域网地址。
- 首次使用必须明确告知：读取的是用户本机微信数据或用户自己导出的聊天数据。
- 默认只输出联系人画像快照和代表性样本，不把全量原始消息写入 Core API。
- 支持 dry-run：只扫描、只预览、不导入。
- 支持数据最小化：用户可选择样本条数、时间范围、排除群聊、排除敏感联系人。
- 支持删除连接器缓存、清理本地索引。

明确不做：

- 不做后台静默读取。
- 不做未授权账号数据读取。
- 不把微信密钥、解密数据库、原始聊天库上传到服务端。
- 不默认绕过系统权限；需要管理员权限或外部工具时必须由用户显式执行。

## 总体架构

新增一个本地连接器包：

```text
apps/wechat-connector/
  package.json
  src/
    main.ts
    server.ts
    config.ts
    auth.ts
    connectors/
      provider.interface.ts
      manual-json.provider.ts
      chatlab-export.provider.ts
      wechat-decrypt-http.provider.ts
      wechat-decrypt-files.provider.ts
    domain/
      contact-index.ts
      message-sampler.ts
      topic-extractor.ts
      bundle-builder.ts
      privacy-redactor.ts
    storage/
      local-cache.ts
      cache-schema.ts
    routes/
      health.route.ts
      scan.route.ts
      contacts.route.ts
      bundles.route.ts
      config.route.ts
    tests/
      fixtures/
      *.test.ts
```

运行方式：

```bash
pnpm dev:wechat-connector
pnpm --filter @yinjie/wechat-connector dev
```

服务地址：

```text
GET  /health
GET  /api/config
PATCH /api/config
POST /api/scan
GET  /api/contacts
POST /api/contact-bundles
POST /api/cache/clear
```

前端仍走 `apps/admin/src/lib/wechat-local-connector.ts`，但需要补齐 pairing token、配置页和更明确的状态呈现。

## 本地连接器安全模型

### 绑定与 CORS

- 默认 `host = 127.0.0.1`，默认 `port = 17364`。
- CORS 只允许：
  - `http://127.0.0.1:5181`
  - `http://localhost:5181`
  - 后续桌面壳内置管理后台 origin
- 禁止 `*`。

### Pairing Token

首次启动连接器生成本地 token：

```text
%APPDATA%/Yinjie/wechat-connector/config.json
```

`GET /health` 可匿名返回：

```json
{
  "ok": true,
  "version": "0.1.0",
  "requiresPairing": true,
  "pairingHint": "请在本地终端复制配对码"
}
```

敏感接口必须带：

```text
X-Yinjie-Connector-Token: <token>
```

管理后台增加“连接器配对码”输入，保存到 localStorage。

### 本地缓存

缓存目录：

```text
%APPDATA%/Yinjie/wechat-connector/cache/
```

缓存内容：

- 联系人索引摘要。
- 消息采样摘要。
- 最近扫描状态。

默认不缓存：

- 解密密钥。
- 原始数据库副本。
- 全量原始聊天记录。

## 数据源 Provider 设计

统一接口：

```ts
interface WechatDataProvider {
  key: string;
  label: string;
  detect(): Promise<ProviderDetectionResult>;
  scan(options: ScanOptions): Promise<ScanResult>;
  listContacts(query: ContactQuery): Promise<ConnectorContactSummary[]>;
  buildBundles(usernames: string[]): Promise<WechatSyncContactBundle[]>;
}
```

### Provider 1：manual-json

目的：

- 保留现有“选择本地 JSON / 粘贴 JSON”能力。
- 让连接器也能读取本地 `WechatSyncContactBundle[]` 文件，并统一提供 `/api/contacts` 与 `/api/contact-bundles`。

输入：

- `WechatSyncContactBundle[]`
- 或更宽松的本地导出 JSON，经过 normalize 后转为 bundle。

优先级：

- P0 必做。
- 最快让 `17364` 服务真实可达。

### Provider 2：chatlab-export

目的：

- 支持用户从 ChatLab 或类似工具导出的标准化聊天数据。
- 不依赖 ChatLab 代码，只读取用户导出的文件。

输入候选：

- JSON/CSV 会话导出。
- 字段映射配置：
  - `contactId`
  - `displayName`
  - `sender`
  - `timestamp`
  - `text`
  - `isGroup`

处理：

- 流式读取大文件。
- 聚合联系人。
- 为每个联系人生成消息计数、双方发言比例、最近消息时间、代表性样本。

优先级：

- P1。适合合法导出路径，风险低。

### Provider 3：wechat-decrypt-http

目的：

- 适配用户外部运行的 `wechat-decrypt`。
- 不 vendor 它的代码，只通过 HTTP 本地接口读取用户授权产生的数据。

外部预期：

- 用户另行运行 `python main.py`，默认 Web UI/API 为 `localhost:5678`。
- README 中公开的 API 包括 `/api/history`、`/api/tags`、`/stream`。

适配策略：

- 连接器配置 `wechatDecryptBaseUrl = http://127.0.0.1:5678`。
- `scan()` 拉取最近会话/历史与标签，构建联系人索引。
- 如果外部 API 无法直接列联系人，则从历史消息中的 chat/session 反推联系人摘要。
- 对大数据量采用分页和增量 `since`。

风险：

- `wechat-decrypt` API 不一定稳定。
- 其核心能力涉及微信数据库解密和进程内存读取，需要用户自己显式运行。
- 不在本项目内隐藏或自动执行这一步。

优先级：

- P2。是“一键本地真实读取微信”的关键适配器，但必须清楚边界。

### Provider 4：wechat-decrypt-files

目的：

- 读取 `wechat-decrypt` 生成的解密输出目录或导出文件。

输入：

```text
decrypted_dir/
all_keys.json
config.json
```

策略：

- 只读取用户指定目录。
- 只解析必要表或中间导出。
- 先做只读连接，不复制数据库。

优先级：

- P3。数据库 schema 变化风险高，放在 HTTP adapter 后面。

## `WechatSyncContactBundle` 生成规则

已有契约：

```ts
interface WechatSyncContactBundle {
  username: string;
  displayName: string;
  nickname?: string | null;
  remarkName?: string | null;
  region?: string | null;
  source?: string | null;
  tags: string[];
  isGroup: boolean;
  messageCount: number;
  ownerMessageCount: number;
  contactMessageCount: number;
  latestMessageAt?: string | null;
  chatSummary?: string | null;
  topicKeywords: string[];
  sampleMessages: WechatSyncMessageSample[];
  momentHighlights: WechatSyncMomentHighlight[];
}
```

生成策略：

- `username`：来源平台稳定 ID；没有则使用 `wechat:<hash(displayName + source)>`。
- `displayName`：优先备注名，其次昵称，其次会话名。
- `tags`：来自微信标签、ChatLab 标签、文件夹/导出标签，统一去重。
- `messageCount`：总有效文本消息数。
- `ownerMessageCount` / `contactMessageCount`：按方向聚合。
- `sampleMessages`：默认最多 16 条，覆盖最近消息、高频话题、情绪强度、问答样本。
- `chatSummary`：P0 使用启发式摘要；P1 可接本地 LLM 或 Core API 预览阶段生成。
- `topicKeywords`：先用 TF-IDF/词频启发式，后续可接本地模型。
- `momentHighlights`：除非数据源明确提供，否则为空。

敏感信息处理：

- 默认过滤手机号、身份证、银行卡、地址、验证码、URL token。
- UI 提供“显示原文样本 / 使用脱敏样本”的开关。
- 默认导入到 Core API 的样本为脱敏版本。

## 管理后台改造

现有页面增强：

- 连接器卡片：
  - 未探测 / 已连接 / 需配对 / 扫描中 / 扫描失败。
  - 显示 provider 类型：`manual-json`、`chatlab-export`、`wechat-decrypt-http`。
  - 配对 token 输入。
- 数据源配置：
  - 文件导入路径。
  - ChatLab 导出文件选择。
  - `wechat-decrypt` API 地址。
  - 时间范围、联系人上限、样本条数、是否包含群聊。
- 扫描结果：
  - 联系人列表。
  - 风险提示：样本少、群聊、联系人 ID 不稳定、敏感信息过多。
  - 支持批量选择、按标签筛选、搜索。
- 导入预览：
  - 继续复用现有预览编辑、批量编辑、审计、恢复、回滚能力。

## Core API 改造

尽量少改 Core API，因为导入逻辑已经完整。

需要补：

- 在 `packages/contracts/src/wechat-sync.ts` 增加连接器响应类型，避免前端私有类型继续膨胀。
- 在 `api/src/modules/admin/wechat-sync-admin.service.ts` 增加 `sourceMetadata` 字段透传，记录数据源：
  - provider
  - connectorVersion
  - scanId
  - importedSamplePolicy
  - redactionEnabled
- 导入历史页展示来源信息。

不需要把本地微信读取能力放入 Core API：

- Core API 可能部署在远端或 Docker 内，不应直接读用户电脑微信数据。
- 本地连接器应独立运行在用户电脑。

## 开发阶段

### P0：让 `17364` 真正存在

目标：

- 新增 `apps/wechat-connector`。
- 实现健康检查、配对 token、manual-json provider、联系人列表、bundle 构建。
- 接入 `dev-services.mjs` 和根脚本。

文件：

- `apps/wechat-connector/package.json`
- `apps/wechat-connector/src/*`
- `scripts/dev-services.mjs`
- `package.json`
- `apps/admin/src/lib/wechat-local-connector.ts`
- `packages/contracts/src/wechat-sync.ts`

验收：

```bash
pnpm dev:wechat-connector
curl http://127.0.0.1:17364/health
pnpm --filter @yinjie/wechat-connector typecheck
pnpm --filter @yinjie/admin typecheck
```

### P1：文件导出路径可用

目标：

- 支持导入 ChatLab/通用聊天导出 JSON/CSV。
- 能从文件里聚合联系人、消息计数、样本、关键词。
- 管理后台不需要手写 JSON，可选择文件后扫描出联系人列表。

验收：

- 使用测试夹具导入 3 个联系人、1 个群聊。
- 管理后台选择联系人后生成预览。
- `POST /api/contact-bundles` 返回有效 `WechatSyncContactBundle[]`。

### P2：适配外部 `wechat-decrypt`

目标：

- 用户外部运行 `wechat-decrypt`。
- 连接器配置 `wechatDecryptBaseUrl`。
- 连接器从外部 API 拉取历史/标签并生成联系人索引。

验收：

- `wechat-decrypt` 不存在时给出清晰错误。
- `wechat-decrypt` 存在时可扫描最近 N 天联系人。
- 不把外部工具密钥或数据库路径暴露到 Core API。

### P3：生产级体验

目标：

- 扫描任务进度、取消、日志。
- 本地缓存管理。
- 数据脱敏报告。
- 大数据性能优化。
- 导入后审计页展示来源和样本策略。

验收：

- 10 万条消息 fixture 扫描不阻塞 UI。
- 失败可重试。
- 删除缓存后本地无残留索引。

## 测试策略

连接器单元测试：

- provider detect。
- JSON/CSV parser。
- contact index。
- message sampler。
- privacy redactor。
- bundle builder。

连接器集成测试：

- 启动本地 server，调用 `/health`、`/api/scan`、`/api/contacts`、`/api/contact-bundles`。
- token 缺失返回 401。
- 非允许 Origin 被 CORS 拒绝。

管理后台测试：

- 连接器未启动时不阻塞手动导入。
- 已配对连接器可扫描并选择联系人。
- 预览 / 导入按钮状态正确。

后端测试：

- `wechat-sync/preview` 接收带 source metadata 的 bundle。
- `wechat-sync/import` 保存来源快照。
- 回滚和恢复不丢 source metadata。

最小验证命令：

```bash
pnpm --filter @yinjie/wechat-connector typecheck
pnpm --filter @yinjie/wechat-connector test
pnpm --filter @yinjie/admin typecheck
pnpm --filter @yinjie/admin build
pnpm --dir api build
```

## 关键风险

### 微信数据来源风险

风险：

- 微信版本变化导致数据库 schema 或加密方式变化。
- 外部工具需要管理员权限。
- 用户误以为项目偷偷读微信。

处理：

- 默认不内置内存读取。
- 只做本地显式配置和外部工具适配。
- UI 强提醒“你正在读取本机数据”。

### 许可证风险

风险：

- ChatLab 是 AGPL-3.0。
- wechat-decrypt 未确认可复用许可证。

处理：

- 不复制源码。
- 不 vendor 依赖。
- 仅支持用户本地外部运行/导出结果适配。
- 如果后续要内置第三方代码，先单独做 license review。

### 隐私风险

风险：

- 原始聊天记录、敏感信息进入 Core API。

处理：

- 默认摘要化和脱敏。
- 导入前展示样本。
- 允许用户删除样本或改成只导入联系人资料。
- 本地缓存可一键清理。

## 推荐实现顺序

1. 先做 P0，解决当前 `17364` 不存在的问题。
2. 再做 P1，让用户通过本地导出文件完成真实扫描，不依赖解密工具。
3. 再做 P2，适配外部 `wechat-decrypt`，实现真正接近“一键”的本机微信读取。
4. 最后做 P3，把任务进度、脱敏、缓存、审计做扎实。

## 最终用户流程

### 无解密工具路径

1. 启动管理后台和本地连接器。
2. 在连接器配置中选择 ChatLab/通用聊天导出文件。
3. 点击“扫描本地数据”。
4. 在联系人列表选择要导入的人。
5. 生成角色预览。
6. 编辑确认。
7. 导入并建立好友关系。

### 外部 wechat-decrypt 路径

1. 用户自行启动 `wechat-decrypt`。
2. 启动 `pnpm dev:wechat-connector`。
3. 管理后台配对连接器。
4. 填入 `wechat-decrypt` 本地地址。
5. 点击“扫描微信联系人”。
6. 选择联系人并生成预览。
7. 导入。

## 成功标准

- 用户不再看到“服务根本不存在”的死路。
- 没有外部解密工具时，也能通过文件导入完成真实本地同步。
- 有外部 `wechat-decrypt` 时，可以通过本地连接器完成联系人列表扫描和批量导入。
- Core API 只接收经过用户确认的 `WechatSyncContactBundle[]`。
- 整个链路有清晰权限、日志、缓存清理和回滚能力。

